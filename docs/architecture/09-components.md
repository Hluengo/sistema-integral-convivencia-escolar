# 09 — Components

Esta sección cubre componentes reutilizables (`shared/ui`, `shared`),
widgets y el árbol completo de componentes de `features/`.

El árbol completo de componentes está documentado en
[Components](components.md).

Este archivo funciona únicamente como índice numerado; el documento
detallado es la fuente de verdad.
